import 'package:flutter/material.dart';

class PendataanPage extends StatelessWidget {
  const PendataanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Pendataan Page'),
      ),
    );
  }
}