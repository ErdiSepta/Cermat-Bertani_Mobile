import 'package:flutter/material.dart';

class PemantauanPage extends StatelessWidget {
  const PemantauanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Pemantauan Page'),
      ),
    );
  }
}